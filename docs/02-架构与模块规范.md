# 架构与模块规范

## 1. 总体架构

项目使用 SwiftUI + MVVM。View 负责界面和交互，ViewModel 负责状态、持久化、刷新和收益计算，Service 负责网络接口和数据解析，Model 负责可持久化或可渲染的数据结构。

```text
SwiftUI View
  -> ViewModel (@MainActor ObservableObject)
    -> Service / Provider
      -> External API / Bridge
    -> UserDefaults / Keychain
    -> Portfolio Sync API
```

## 2. 目录与文件职责

```text
FundValuationApp/
├── FundValuationApp.swift
├── MainView.swift
├── FundModels.swift
├── FundServices.swift
├── FundViewModel.swift
├── StockModels.swift
├── StockServices.swift
├── StockViewModel.swift
└── StockViews.swift

ths-bridge/
├── app.py
├── requirements.txt
└── README.md
```

| 文件 | 职责 |
|---|---|
| `MainView.swift` | 根页面、顶部栏、基金页面、设置页、基金详情/编辑、全局 Tab 切换 |
| `AuthViews.swift` | 登录、注册、找回密码和账号服务地址配置 |
| `AuthService.swift` | 账号与持仓同步 HTTP 客户端 |
| `SessionViewModel.swift` | 会话状态、Keychain token、首次迁移和持仓同步 |
| `KeychainStore.swift` | 访问系统 Keychain 保存 access token |
| `FundModels.swift` | 基金持仓、快照、汇总、走势范围、AI 配置 |
| `FundServices.swift` | 基金估值/净值/走势接口、日期工具、数字格式化 |
| `FundViewModel.swift` | 基金持仓 CRUD、刷新、快照计算、AI 调度、本地缓存 |
| `StockModels.swift` | 美股持仓、行情、搜索结果、快照、汇总、K 线、Provider 配置 |
| `StockServices.swift` | 美股 Provider 协议和 Finnhub/Alpha Vantage/THS Bridge/Mock 实现 |
| `StockViewModel.swift` | 美股持仓 CRUD、行情刷新、搜索、K 线缓存、排序、Provider 配置 |
| `StockViews.swift` | 美股首页、列表、详情、编辑、K 线等 UI |
| `ths-bridge` | iOS 与同花顺/上游行情之间的轻量 HTTP Bridge |

## 3. 模块边界

### 3.1 基金模块

基金模块只处理场外基金：

- 以基金代码为唯一业务键。
- 当前价格是估值或净值。
- 收益按单位净值变化乘以份额计算。
- 走势对比基准是沪深300。
- AI 分析当前仅面向基金组合。

基金逻辑不要依赖 `StockViewModel` 或美股 Provider。

### 3.2 美股模块

美股模块只处理 US 股票持仓：

- 以股票 symbol 为唯一业务键。
- 主价格使用 regular price。
- 当日盈亏基于 `regularPrice - previousClose`。
- 累计盈亏基于 `regularPrice - averageCost`。
- 盘前/盘后数据只作为行情补充，不覆盖主收益口径。

股票逻辑不要写进 `FundViewModel`。

### 3.3 MainView 边界

`MainView` 可以组合基金和美股页面，但不应该承载美股业务计算。新增股票能力时优先放入：

- Model -> `StockModels.swift`
- Provider / API -> `StockServices.swift`
- 状态和计算 -> `StockViewModel.swift`
- UI -> `StockViews.swift`

## 4. 导航规范

当前主要使用 `fullScreenCover` 打开详情、编辑、设置和 AI 页面。所有全屏页需要：

- 自己提供顶部导航。
- 支持取消/返回。
- 支持左侧边缘右滑返回。
- 进入编辑页时带入历史数据。
- 取消编辑后回到原页面状态，不二次弹出详情页。

## 5. Tab 切换规范

基金/美股是资产类型切换，不是底部 Tab Bar。顶部使用分段控件。

当前实现使用垂直 `ScrollView` 承载内容，并通过横向拖拽切换 `selectedTab`。后续如果重新做页面级横滑，需要优先验证：

- 不裁剪底部内容。
- 不和列表点击冲突。
- 横滑时页面整体移动。
- 点击顶部 Tab 和横滑动画一致。

不要在没有真机验证的情况下再次引入会裁剪页面高度的分页容器。
