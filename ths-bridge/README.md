# ths-bridge has moved

The backend service has been split out of the iOS repository.

Use the standalone backend project instead:

```text
/Users/billzhang/GitHub/Fund Valuation Backend
```

The existing Zeabur service has been repointed to the standalone backend repository. The public production base URL and `/v1/*` API contract remain unchanged:

```text
https://thsbridge.zeabur.app
```

This directory is kept only as a migration pointer so older documentation and local notes have an obvious landing page.
